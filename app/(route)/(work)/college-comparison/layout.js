export const metadata = {
  title: "College Comparison",
  description:
    "Compare colleges, scholarships, exams, and more with College Finder. Find the perfect academic match for your future.",
};

export default function CollegeComparisonLayout({ children }) {
  return <>{children}</>;
}
