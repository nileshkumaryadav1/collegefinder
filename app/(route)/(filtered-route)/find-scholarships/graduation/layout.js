export const metadata = {
  title: "Graduation Scholarships",
  description:
    "Explore top scholarships, colleges, exams, and more with College Finder. Find the perfect academic match for your future.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
