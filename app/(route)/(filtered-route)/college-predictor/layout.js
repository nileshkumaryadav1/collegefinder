export const metadata = {
  title: "College Predictor",
  description: "Predict best College for you with College Finder.",
};

export default function RankPredictorLayout({ children }) {
  return <>{children}</>;
}
