export const metadata = {
  title: "IITs Colleges",
  description:
    "Explore top IIT colleges, universities, courses, fees, placements, and more with College Finder. Find the perfect academic match for your future.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
