export const metadata = {
  title: "NITs Colleges",
  description:
    "Explore top NIT colleges, universities, courses, fees, placements, and more with College Finder. Find the perfect academic match for your future.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
