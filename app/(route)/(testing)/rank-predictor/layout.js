export const metadata = {
  title: "Rank Predictor",
  description:
    "Predict your rank in colleges, scholarships, exams, and more with College Finder.",
};

export default function RankPredictorLayout({ children }) {
  return <>{children}</>;
}
